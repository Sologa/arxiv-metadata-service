# Configuration
db:
  path: "data/arxiv_meta.db"        # SQLite FTS5 database path

server:
  host: "0.0.0.0"                   # Listen address
  port: 8110                        # Port

data:
  dir: "data/"                      # Data directory
  jsonl: "data/arxiv_metadata.jsonl"     # Kaggle JSON Lines file
  jsonl_gz: "data/arxiv_metadata.jsonl.gz"  # Compressed version
  
kaggle:
  dataset: "Cornell-University/arxiv"   # Kaggle dataset ID
